# 龙虾道具交易平台 - 技术部署文档 v1.0

> 📅 更新日期：2026-05-16  
> 📁 项目路径：`C:\Users\Administrator\.openclaw\workspace\projects\龙虾道具交易平台\code\`

---

## 一、项目结构

```
code/
├── lobster-trade-server/          # 后端 Spring Boot（JAR包 58.6MB）
│   ├── src/main/java/             # Java源码（含 payment/ 配置）
│   ├── src/main/resources/
│   │   ├── application.yml        # 主配置（生产用，环境变量注入）
│   │   ├── application-dev.yml    # 开发配置（debug日志）
│   │   ├── payment-config-template.yml  # 支付配置模板
│   │   ├── wechat_private_key.pem       # 微信私钥文件（可选）
│   │   └── mapper/                     # MyBatis XML
│   ├── db/
│   │   ├── init_database.sql      # 创建数据库 lobster_trade
│   │   ├── init.sql               # 建表脚本（30张表）
│   │   └── seed_data.sql          # 初始数据（游戏/测试用户/管理员）
│   ├── sql/
│   │   ├── init.sql               # 同上
│   │   ├── admin_table.sql / admin_init.sql / admin_exec.sql
│   │   ├── cs_tables.sql          # 客服相关表
│   │   ├── im_tables.sql          # IM相关表
│   │   ├── certification_table.sql  # 服务商认证表
│   │   ├── sprint2_init.sql       # Sprint2 增强表
│   │   └── sprint3_hot_search.sql  # 热搜词表
│   ├── pom.xml                   # Maven（Spring Boot 3.2.5 / JDK 17）
│   ├── start.bat                 # Windows 启动脚本（加载 .env）
│   ├── start.sh                  # Linux 启动脚本
│   └── .env                      # 开发环境变量
│
├── lobster-trade-web/            # 用户端（Vue 3 + Element Plus + Vite）
│   ├── src/
│   │   ├── api/                  # API 调用封装
│   │   ├── views/                # 页面（home/detail/order/publish/wallet/cs...）
│   │   ├── stores/               # Pinia 状态管理
│   │   └── router/               # 路由配置
│   ├── package.json
│   └── vite.config.js            # 开发: localhost:5174，代理 /api → 8080
│
├── lobster-trade-admin/          # 管理后台（Vue 3 + Element Plus + Vite）
│   ├── src/
│   ├── package.json
│   └── vite.config.js            # 开发: localhost:8090，代理 /api/admin → 8080
│
├── lobster-trade-h5/             # H5端（Vue 3 + Vant + Vite）
│   ├── .env                      # VITE_API_BASE_URL=/api
│   ├── package.json
│   └── vite.config.js            # 开发: localhost:3000，代理 /api → 8080
│
├── docker-compose.yml            # Docker 编排（MySQL + Redis + 后端 + 3前端）
├── build.sh                      # 构建（后端 mvn + 前端 npm run build）
├── deploy.sh                     # 部署（加载 .env.production + docker compose up）
│
├── .env.production               # 生产环境变量（docker-compose 用）
│
├── nginx-web.conf               # 用户端 Nginx 配置（端口80）
├── nginx-admin.conf             # 管理后台 Nginx 配置（端口81）
├── nginx-h5.conf                # H5端 Nginx 配置（端口3000）
│
├── PAYMENT_INTEGRATION.md        # 支付渠道配置详解
├── DEPLOYMENT_GUIDE.md           # 部署指南（参考）
└── db-config-guide.md            # 数据库安全规范
```

---

## 二、服务器环境要求

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | **17+** | Spring Boot 3.2.5 强制要求 |
| Maven | 3.8+ | 构建后端 |
| Node.js | 18+ | 构建前端 |
| MySQL | **8.0+** | 数据库（字符集 utf8mb4） |
| Redis | 6.0+ | 缓存 + Session |
| Docker | 24+ | 可选，容器部署 |
| Nginx | 1.18+ | 前端 Web 服务 |

---

## 三、数据库部署（必须按顺序）

```bash
# ★ 第一步：创建数据库（仅需执行一次）
mysql -u root -p < lobster-trade-server/db/init_database.sql

# ★ 第二步：创建全部表（30张）
mysql -u root -p lobster_trade < lobster-trade-server/db/init.sql

# ★ 第三步：初始化基础数据（游戏分类/测试用户/管理员/热搜词/优惠券）
mysql -u root -p lobster_trade < lobster-trade-server/db/seed_data.sql

# ★ 第四步（如需额外功能表）：
mysql -u root -p lobster_trade < lobster-trade-server/sql/cs_tables.sql
mysql -u root -p lobster_trade < lobster-trade-server/sql/im_tables.sql
mysql -u root -p lobster_trade < lobster-trade-server/sql/certification_table.sql
mysql -u root -p lobster_trade < lobster-trade-server/sql/sprint2_init.sql
mysql -u root -p lobster_trade < lobster-trade-server/sql/sprint3_hot_search.sql
```

### 数据库账号创建

```sql
-- 创建专用账号（生产环境禁止用 root）
CREATE DATABASE lobster_trade DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER 'lobster'@'%' IDENTIFIED BY '此处填入强密码';  -- 例：Lobster@2026!Abc
GRANT ALL PRIVILEGES ON lobster_trade.* TO 'lobster'@'%';
FLUSH PRIVILEGES;
```

### 数据库表清单（共30张）

| 序号 | 表名 | 说明 |
|------|------|------|
| 1 | user | 用户表 |
| 2 | user_session | 用户会话 |
| 3 | user_login_device | 登录设备 |
| 4 | user_real_name | 实名认证 |
| 5 | wallet | 钱包 |
| 6 | wallet_transaction | 钱包流水 |
| 7 | sms_code | 短信验证码 |
| 8 | admin | 管理员（老） |
| 9 | admin_user | 管理员 |
| 10 | admin_role | 管理员角色 |
| 11 | admin_audit_log | 操作审计日志 |
| 12 | game | 游戏（旧） |
| 13 | game_category | 游戏分类 |
| 14 | product_category | 商品服务分类 |
| 15 | product | 商品 |
| 16 | trade_order | 订单 |
| 17 | order_progress | 订单进度 |
| 18 | trade_review | 评价 |
| 19 | im_session | IM会话 |
| 20 | im_message | IM消息 |
| 21 | cs_session | 客服会话 |
| 22 | cs_message | 客服消息 |
| 23 | coupon | 优惠券 |
| 24 | user_coupon | 用户优惠券 |
| 25 | hot_search_word | 热搜词 |
| 26 | platform_announcement | 平台公告 |
| 27 | sys_notification | 系统通知 |
| 28 | service_provider_certification | 服务商认证 |
| 29 | service_ticket | 客服工单 |
| 30 | product_recommend_slot | 推荐位 |

---

## 四、环境变量完整清单

### 4.1 基础配置（必须）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `SPRING_PROFILES_ACTIVE` | `prod` | 生产环境 |
| `DB_HOST` | `localhost` 或 `rm-xxxx.mysql.rds.aliyuncs.com` | 数据库地址 |
| `DB_PORT` | `3306` | 数据库端口 |
| `DB_NAME` | `lobster_trade` | 数据库名 |
| `DB_USERNAME` | `lobster` | 数据库用户名 |
| `DB_PASSWORD` | `Lobster@2026!Abc` | **强密码（必须）** |
| `DB_SSL` | `true` | 生产环境建议 true |
| `REDIS_HOST` | `localhost` 或 `r-xxxx.redis.rds.aliyuncs.com` | Redis 地址 |
| `REDIS_PORT` | `6379` | Redis 端口 |
| `REDIS_PASSWORD` | `Redis@2026!Abc` | Redis 密码（无则留空） |

### 4.2 JWT 与支付密钥（必须）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `JWT_SECRET` | `生成至少32位随机字符串` | JWT 签名密钥 |
| `JWT_EXPIRE` | `86400000` | Token 过期（毫秒，默认1天） |
| `PAYMENT_SECRET` | `生成至少32位随机字符串` | 支付加密密钥 |

### 4.3 服务配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `SERVER_PORT` | `8080` | 后端端口 |
| `UPLOAD_PATH` | `/data/uploads/lobster` | 上传文件存储路径 |
| `UPLOAD_BASE_URL` | `https://您的域名/uploads` | 文件访问基础URL |
| `LOG_LEVEL` | `info` | 日志级别（debug/info/warn/error） |

### 4.4 支付宝（AppID 已申请：2021006151687443）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `ALIPAY_ENABLED` | `true` | 生产环境设为 true |
| `ALIPAY_APP_ID` | `2021006151687443` | 支付宝应用 AppID |
| `ALIPAY_PRIVATE_KEY` | `MIIEuwIBADANBgk...` | **应用私钥（PKCS8 RSA2）** |
| `ALIPAY_PUBLIC_KEY` | `MIIBIjANBgk...` | **支付宝公钥** |
| `ALIPAY_NOTIFY_URL` | `https://您的域名/api/payment/alipay/notify` | 异步回调（必须HTTPS） |
| `ALIPAY_RETURN_URL` | `https://您的域名/payment/result` | 前端回跳地址 |
| `ALIPAY_SANDBOX` | `false` | 生产环境 false |

**私钥生成命令：**
```bash
# 生成 RSA2 私钥
openssl genrsa -out app_private_key.pem 2048

# 转换为 PKCS8（必须！）
openssl pkcs8 -topk8 -inform PEM -outform PEM -nocrypt -in app_private_key.pem -out app_private_key_pkcs8.pem

# 查看私钥内容
cat app_private_key_pkcs8.pem
```

### 4.5 微信支付

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `WECHAT_ENABLED` | `true` | 生产环境设为 true |
| `WECHAT_APP_ID` | `wx...` | 微信 AppID |
| `WECHAT_MCH_ID` | `1234567890` | 8位数字商户号 |
| `WECHAT_PRIVATE_KEY` | `-----BEGIN PRIVATE KEY-----...` | **APIv3 私钥内容（PEM格式）** |
| `WECHAT_API_KEY` | `APIv2 HMAC-SHA256密钥` | APIv2 密钥（可选） |
| `WECHAT_CERT_SERIAL_NO` | `XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX` | **平台证书序列号（40位十六进制）** |
| `WECHAT_CERT_CONTENT` | `-----BEGIN CERTIFICATE-----...` | **平台证书完整PEM内容** |
| `WECHAT_NOTIFY_URL` | `https://您的域名/api/payment/wechat/notify` | 异步回调（必须HTTPS） |

### 4.6 银行卡/聚合支付（预留，暂未接入）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `BANKCARD_ENABLED` | `false` | 保持 false |
| `BANKCARD_PLATFORM` | `laKaLa` / `EasyPay` / `unionPay` | 支付平台 |
| `BANKCARD_MCH_ID` | `商户号` | 商户号 |
| `BANKCARD_APP_ID` | `应用ID` | 应用ID |
| `BANKCARD_NOTIFY_URL` | `https://您的域名/api/payment/bankcard/notify` | 回调地址 |

---

## 五、后端配置（application.yml）

文件位置：`lobster-trade-server/src/main/resources/application.yml`

**核心原则：所有敏感字段通过环境变量注入，禁止硬编码。**

```yaml
spring:
  application:
    name: lobster-trade-server

  profiles:
    active: dev   # 生产设为 prod

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:lobster_trade}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=${DB_SSL:false}&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 20MB

  web:
    resources:
      static-locations:
        - classpath:/META-INF/resources/
        - classpath:/resources/
        - classpath:/static/
        - classpath:/public/

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: ${MYBATIS_LOG_IMPL:org.apache.ibatis.logging.stdout.StdOutImpl}
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.lobster.trade.model.entity

jwt:
  secret: ${JWT_SECRET:}
  expire: ${JWT_EXPIRE:86400000}

payment:
  secret: ${PAYMENT_SECRET:}

  alipay:
    enabled: ${ALIPAY_ENABLED:false}
    appId: ${ALIPAY_APP_ID:}
    privateKey: ${ALIPAY_PRIVATE_KEY:}
    alipayPublicKey: ${ALIPAY_PUBLIC_KEY:}
    notifyUrl: ${ALIPAY_NOTIFY_URL:http://localhost:8080/api/payment/alipay/notify}
    returnUrl: ${ALIPAY_RETURN_URL:http://localhost:5174/payment/result}
    sandbox: ${ALIPAY_SANDBOX:false}

  wechat:
    enabled: ${WECHAT_ENABLED:false}
    appId: ${WECHAT_APP_ID:}
    mchId: ${WECHAT_MCH_ID:}
    privateKey: ${WECHAT_PRIVATE_KEY:}
    privateKeyPath: ${WECHAT_PRIVATE_KEY_PATH:}
    apiKey: ${WECHAT_API_KEY:}
    certSerialNo: ${WECHAT_CERT_SERIAL_NO:}
    certContent: ${WECHAT_CERT_CONTENT:}
    notifyUrl: ${WECHAT_NOTIFY_URL:http://localhost:8080/api/payment/wechat/notify}

  bankcard:
    enabled: ${BANKCARD_ENABLED:false}
    platform: ${BANKCARD_PLATFORM:}
    mchId: ${BANKCARD_MCH_ID:}
    appId: ${BANKCARD_APP_ID:}
    privateKey: ${BANKCARD_PRIVATE_KEY:}
    publicKey: ${BANKCARD_PUBLIC_KEY:}
    notifyUrl: ${BANKCARD_NOTIFY_URL:}

server:
  port: ${SERVER_PORT:8080}

upload:
  path: ${UPLOAD_PATH:D:/uploads/lobster}
  base-url: ${UPLOAD_BASE_URL:http://localhost:8080/uploads}

logging:
  level:
    com.lobster.trade: ${LOG_LEVEL:debug}
```

---

## 六、后端启动方式

### 方式一：直接运行（JAR）

```bash
cd lobster-trade-server

# Linux 设置环境变量
export SPRING_PROFILES_ACTIVE=prod
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=lobster_trade
export DB_USERNAME=lobster
export DB_PASSWORD=此处填入强密码
export REDIS_HOST=localhost
export REDIS_PORT=6379
export JWT_SECRET=此处填入至少32位随机字符串
export PAYMENT_SECRET=此处填入至少32位随机字符串
export ALIPAY_ENABLED=true
export ALIPAY_APP_ID=2021006151687443
export ALIPAY_PRIVATE_KEY=此处填入PKCS8私钥
export ALIPAY_PUBLIC_KEY=此处填入支付宝公钥
export ALIPAY_NOTIFY_URL=https://您的域名/api/payment/alipay/notify

# 启动
java -Xms512m -Xmx1024m -jar target/lobster-trade-server-1.0.0-SNAPSHOT.jar
```

### 方式二：start.bat（Windows 开发）

```bat
cd lobster-trade-server
start.bat
```
> start.bat 会自动从 `.env` 文件加载环境变量并启动

### 方式三：start.sh（Linux 生产）

```bash
cd lobster-trade-server
chmod +x start.sh
./start.sh
```

### 方式四：Systemd 服务（Linux 生产推荐）

```bash
# /etc/systemd/system/lobster-trade.service
[Unit]
Description=龙虾道具交易平台后端
After=network.target mysql.service redis.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/lobster-trade-server
Environment="SPRING_PROFILES_ACTIVE=prod"
Environment="DB_HOST=localhost"
Environment="DB_PORT=3306"
Environment="DB_NAME=lobster_trade"
Environment="DB_USERNAME=lobster"
Environment="DB_PASSWORD=此处填入强密码"
Environment="REDIS_HOST=localhost"
Environment="REDIS_PORT=6379"
Environment="JWT_SECRET=此处填入JWT密钥"
Environment="PAYMENT_SECRET=此处填入支付密钥"
Environment="ALIPAY_ENABLED=true"
Environment="ALIPAY_APP_ID=2021006151687443"
Environment="ALIPAY_PRIVATE_KEY=此处填入私钥"
Environment="ALIPAY_PUBLIC_KEY=此处填入公钥"
Environment="ALIPAY_NOTIFY_URL=https://您的域名/api/payment/alipay/notify"
Environment="ALIPAY_SANDBOX=false"
ExecStart=/usr/bin/java -Xms512m -Xmx1024m -jar lobster-trade-server-1.0.0-SNAPSHOT.jar
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload
systemctl enable lobster-trade
systemctl start lobster-trade
systemctl status lobster-trade
```

### 方式五：Docker（docker-compose）

```bash
# 1. 修改 .env.production 中的所有配置项（填入真实值）

# 2. 构建
chmod +x build.sh && ./build.sh

# 3. 部署
chmod +x deploy.sh && ./deploy.sh

# 或手动
docker compose up -d --build
```

---

## 七、前端构建与部署

### 7.1 用户端（lobster-trade-web）

```bash
cd lobster-trade-web
npm install --legacy-peer-deps
npm run build
# 输出：dist/
```

### 7.2 管理后台（lobster-trade-admin）

```bash
cd lobster-trade-admin
npm install --legacy-peer-deps
npm run build
# 输出：dist/
```

### 7.3 H5端（lobster-trade-h5）

```bash
cd lobster-trade-h5
npm install
npm run build
# 输出：dist/
```

### 7.4 Nginx 配置

**用户端（端口 80，nginx-web.conf）：**
```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;   # 替换为实际 dist 路径
    index index.html;

    location /uploads/ {
        proxy_pass http://server:8080;
    }

    location /api/ {
        proxy_pass http://server:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**管理后台（端口 81，nginx-admin.conf）：**
```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;   # 替换为实际 dist 路径
    index index.html;

    location /api/ {
        proxy_pass http://server:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**H5端（端口 3000，nginx-h5.conf）：**
```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;   # 替换为实际 dist 路径
    index index.html;

    location /api/ {
        proxy_pass http://server:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

## 八、服务端口汇总

| 服务 | 端口 | 访问地址 |
|------|------|---------|
| 后端 API | 8080 | `http://localhost:8080` |
| API 文档 | 8080 | `http://localhost:8080/swagger-ui.html` |
| 用户端 Web | 80 | `http://localhost` |
| 管理后台 | 81 | `http://localhost:81` |
| H5端 | 3000 | `http://localhost:3000` |
| MySQL | 3306 | Docker 映射 |
| Redis | 6379 | Docker 映射 |

---

## 九、测试账号

> ⚠️ **生产部署前必须修改所有默认密码！**

| 角色 | 用户名/手机号 | 密码 | 备注 |
|------|-------------|------|------|
| 超级管理员 | `admin` | `admin123` | 拥有所有权限 |
| 运营管理员 | `operator` | `admin123` | 运营权限 |
| 买家 | `13812340001` | `Test123456` | 已实名，银牌用户 |
| 卖家 | `13900002222` | `Test123456` | 已实名，金牌用户 |
| 卖家 | `13900002223` | `Test123456` | 已实名，金牌用户 |

---

## 十、API 接口速查

### 用户端（/api/*）
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 注册 | POST | /api/auth/register | 手机号+密码 |
| 登录 | POST | /api/auth/login | 返回 JWT Token |
| 发送验证码 | POST | /api/auth/send-code | 短信验证码 |
| 游戏分类 | GET | /api/games | 游戏列表 |
| 商品搜索 | GET | /api/product/search | 关键词+筛选分页 |
| 商品详情 | GET | /api/product/{id} | 商品详情 |
| 发布商品 | POST | /api/product/publish | 需要登录 |
| 创建订单 | POST | /api/order/create | 创建订单 |
| 订单支付 | POST | /api/order/pay/{id} | 钱包/支付宝/微信 |
| 提交发货 | POST | /api/order/delivery/{id} | 发货截图+备注 |
| 确认收货 | POST | /api/order/confirm/{id} | 确认完成 |
| 钱包充值 | POST | /api/wallet/recharge | 充值 |
| 钱包提现 | POST | /api/wallet/withdraw | 提现 |
| 钱包余额 | GET | /api/wallet/balance | 余额查询 |
| 客服发起 | POST | /api/cs/start | 发起客服会话 |
| 商品评价 | POST | /api/review/add | 订单评价 |

### 管理后台（/api/admin/*）
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 管理员登录 | POST | /api/admin/login | 返回 Admin Token |
| 用户列表 | GET | /api/admin/user/list | 分页+筛选 |
| 订单列表 | GET | /api/admin/order/list | 分页+状态筛选 |
| 订单强制修改 | POST | /api/admin/order/force-update | 人工介入 |
| 商品列表 | GET | /api/admin/product/list | 商品管理 |
| 实名认证 | GET | /api/admin/real-name/list | 实名审核 |
| 客服会话 | GET | /api/admin/cs/session/list | 客服会话 |
| 公告管理 | CRUD | /api/admin/announcement/* | 平台公告 |
| 热搜词 | CRUD | /api/admin/hot-search/* | 热搜词管理 |
| 优惠券 | CRUD | /api/admin/coupon/* | 优惠券 |
| 统计数据 | GET | /api/admin/stats/* | 运营统计 |

---

## 十一、支付渠道配置

### 11.1 支付宝（已集成 RSA2，AppID: 2021006151687443）

- **回调地址：** `https://您的域名/api/payment/alipay/notify`（必须 HTTPS）
- **私钥格式：** PKCS8 + RSA2
- **沙箱测试：** `ALIPAY_SANDBOX=true`（沙箱网关：`https://openapi-sandbox.dl.alipaydev.com/gateway.do`）
- **生产：** `ALIPAY_SANDBOX=false`，`ALIPAY_ENABLED=true`

### 11.2 微信支付（APIv3）

- **回调地址：** `https://您的域名/api/payment/wechat/notify`（必须 HTTPS）
- **私钥格式：** PKCS#8（PEM）
- **必须配置：** `WECHAT_PRIVATE_KEY`（APIv3私钥）+ `WECHAT_CERT_CONTENT`（平台证书）+ `WECHAT_CERT_SERIAL_NO`（证书序列号）
- **沙箱：** `WECHAT_SANDBOX=true`（需沙箱商户号）

### 11.3 银行卡/聚合支付

预留接口（`BankCardPayGateway`），未接入时 `BANKCARD_ENABLED=false`

### 11.4 Mock支付（测试用）

系统内置 Mock，不依赖任何外部接口。测试时直接用钱包余额支付即可，无需配置支付宝/微信。

---

## 十二、构建脚本

### build.sh（构建所有项目）
```bash
#!/bin/bash
set -e

echo "[1/3] 构建后端..."
cd lobster-trade-server && mvn clean package -DskipTests -q && cd ..

echo "[2/3] 构建用户端..."
cd lobster-trade-web && npm install --legacy-peer-deps && npm run build && cd ..

echo "[3/3] 构建管理后台..."
cd lobster-trade-admin && npm install --legacy-peer-deps && npm run build && cd ..

echo "==== 构建完成 ===="
```

### deploy.sh（Docker 部署）
```bash
#!/bin/bash
set -e

if [ ! -f "lobster-trade-server/target/lobster-trade-server-1.0.0-SNAPSHOT.jar" ]; then
    echo "后端未构建，开始构建..."
    chmod +x build.sh && ./build.sh
fi

set -a && source .env.production && set +a

echo "启动 Docker 服务..."
docker compose up -d --build

echo "==== 部署完成 ===="
echo "用户端: http://localhost"
echo "管理后台: http://localhost:81"
echo "H5端: http://localhost:3000"
echo "后端API: http://localhost:8080"
```

---

## 十三、常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 启动报错"Unable to acquire JDBC Connection" | 数据库连接失败 | 检查 `DB_HOST`、`DB_PORT`、`DB_USERNAME`、`DB_PASSWORD` |
| 前端请求全部 404 | Nginx 代理配置错误 | 检查 `location /api/` 的 `proxy_pass` 指向 `127.0.0.1:8080` |
| 支付回调失败 | 公网不可达/HTTPS/密钥错误 | 检查防火墙；确认支付宝公钥是「支付宝公钥」而非「应用公钥」 |
| 微信支付 SIGN_ERROR | 沙箱签名不同/缺少平台证书 | 确认 `WECHAT_CERT_SERIAL_NO` 和 `WECHAT_CERT_CONTENT` 正确 |
| Redis 连接失败 | 密码错误/端口未开 | 检查 `REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD` |
| 文件上传后无法访问 | 目录无写权限 | 确认 `UPLOAD_PATH` 目录存在且可写；Nginx 配置了 `/uploads/` 代理 |

---

## 十四、生产环境检查清单

- [ ] 数据库密码已修改（禁止弱密码）
- [ ] `JWT_SECRET` 已生成（至少32位随机字符串）
- [ ] `PAYMENT_SECRET` 已生成
- [ ] 支付宝/微信回调地址已改为正式域名（HTTPS）
- [ ] `ALIPAY_ENABLED=true`（生产）
- [ ] `ALIPAY_SANDBOX=false`（生产）
- [ ] `WECHAT_ENABLED=true`（生产）
- [ ] 所有测试账号密码已修改
- [ ] 数据库禁止外网访问（只允许内网 IP）
- [ ] Redis 禁止外网访问
- [ ] HTTPS 已配置（支付宝/微信要求 HTTPS 回调）
- [ ] 防火墙已开放端口：80, 81, 3000, 8080
- [ ] `SPRING_PROFILES_ACTIVE=prod`（关闭 debug 日志）

---

## 十五、日志查看

```bash
# Systemd
journalctl -u lobster-trade -f

# Docker
docker logs lobster-server -f

# 支付回调日志关键词
grep "ALIPAY_NOTIFY\|WECHAT_NOTIFY\|PAYMENT" server.log

# 错误日志
grep "ERROR\|Exception" server.log
```

---

**文档版本：** v1.0  
**编写日期：** 2026-05-16